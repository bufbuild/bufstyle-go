version: "2"
linters:
  default: all
  disable:
    - cyclop            # covered by gocyclo
    - depguard          # unnecessary for small libraries
    - err113            # way too noisy
    - exhaustruct       # many exceptions
    - funlen            # rely on code review to limit function length
    - gochecknoglobals  # many exceptions
    - gocognit          # dubious "cognitive overhead" quantification
    - gocritic          # many exceptions
    - gomodguard        # deprecated in v2.12.0, replaced by gomodguard_v2
    - inamedparam       # not standard style
    - interfacebloat    # many exceptions
    - ireturn           # "accept interfaces, return structs" isn't ironclad
    - lll               # don't want hard limits for line length
    - maintidx          # covered by gocyclo
    - nilnil            # allow this
    - nlreturn          # generous whitespace violates house style
    - noinlineerr       # excess scope violates house style
    - testifylint       # does not want us to use assert
    - testpackage       # internal tests are fine
    - thelper           # we want to print out the whole stack
    - wrapcheck         # don't _always_ need to wrap errors
    - wsl               # generous whitespace violates house style
    - wsl_v5            # generous whitespace violates house style
  settings:
    errcheck:
      check-type-assertions: true
    forbidigo:
      forbid:
        - pattern: '^fmt\.Print'
        - pattern: '^log\.'
        - pattern: '^print$'
        - pattern: '^println$'
        - pattern: '^panic$'
    godox:
      # TODO, OPT, etc. comments are fine to commit. Use FIXME comments for
      # temporary hacks, and use godox to prevent committing them.
      keywords: [FIXME]
    varnamelen:
      ignore-decls:
        - T any
        - i int
        - wg sync.WaitGroup
        - id string
  exclusions:
    generated: lax
    presets:
      - comments
      - common-false-positives
      - legacy
      - std-error-handling
    rules:
      - linters:
          - prealloc
        path: internal/internal.go
formatters:
  # gofumpt is omitted: prefer standard gofmt.
  # goimports is omitted: rely on gci instead.
  enable:
    - gci
    - gofmt
  exclusions:
    generated: lax
